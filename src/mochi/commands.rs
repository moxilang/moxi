/// Built-in Mochi commands
/// 
/// This will eventually include things like:
/// - translate
/// - rotate
/// - merge
/// - grid
/// Right now it’s empty.

pub fn list_commands() -> Vec<&'static str> {
    vec!["translate", "rotate", "merge", "grid"]
}

