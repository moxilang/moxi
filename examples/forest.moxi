voxel Tree {

    [Layer 0]
    .X.
    XXX
    .X.

    [Layer 1]
    XXX
    .X.
    XXX

    [Layer 2]
    🌳🌳🌳
    🌳🍡🌳
    🌳🌳🌳

    [Colors]
    X: brown
    🌳: green
    🍡: mochi-pink
}

# Build the base tree
tree = Tree

# Translate copies of the tree
clone1 = translate(tree, 5, 0, 0)
clone2 = translate(tree, -5, 0, 0)
clone3 = translate(tree, 0, 0, 5)

# Merge them into a forest
forest = merge(tree, clone1, clone2, clone3)

print forest
