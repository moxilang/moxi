# A tree with trunk + foliage
voxel Tree {
    [Layer 0]
    .X.
    XXX
    .X.

    [Layer 1]
    .X.
    XXX
    .X.

    [Layer 2]
    XXX
    .X.
    XXX

    [Layer 3]
    🌳🌳🌳
    🌳🍡🌳
    🌳🌳🌳

    [Layer 4]
    🌳🌳🌳
    🌳🍡🌳
    🌳🌳🌳

    [Colors]
    X: brown
    r: red
    🌳: green
    🍡: mochi-pink
}

# A simple cube rock
voxel Rock {
    [Layer 0]
    XX
    XX

    [Layer 1]
    XX
    XX

    [Colors]
    X: black
}


# --- Instantiate base objects ---

tree   = Tree
rock = Rock

# --- Merge tree + pebble into new composite model ---

merged_model = merge(tree, rock)

# --- Translate merged instance by +10 in x direction ---
treerock1 = translate(merged_model, (x=0, y=0, z=0))
treerock2 = translate(merged_model, (x=5, y=0, z=0))

two_trees = merge(treerock1, treerock2)

trees1 = translate(two_trees, (x=0, y=0, z=0))
trees2 = translate(two_trees, (x=0, y=5, z=0))

small_forest = merge(trees1, trees2)

sforest1 = translate(small_forest, (x=0, y=0, z=0))
sforest2 = translate(small_forest, (x=10, y=0, z=0))
sforest3 = translate(small_forest, (x=20, y=0, z=0))

med_forest = merge(sforest1, sforest2, sforest3)

mforest1 = translate(med_forest, (x=0, y=0, z=0))
mforest2 = translate(med_forest, (x=0, y=10, z=0))

large_forest = merge(mforest1, mforest2)

# --- Show results ---

print tree              # should print Instance details
print large_forest      # should print merged Instance details
print                   # should show: Scene has 4 instances